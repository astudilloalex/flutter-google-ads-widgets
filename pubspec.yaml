name: google_ads_widgets
description: A new Flutter package project.
version: 0.0.2
repository: 

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.3

flutter: